{
  "name": "green-tunnel-gui",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "electron .",
    "package-mac": "electron-packager . --overwrite --asar --platform=darwin --arch=x64 --icon=icons/icon.icns --prune=true --out=release-builds",
    "package-win": "electron-packager . --overwrite --asar --platform=win32 --arch=ia32 --icon=icons/icon.icns --prune=true --out=release-builds",
    "package-linux": "electron-packager . --overwrite --asar --platform=linux --arch=x64 --icon=icons/icon.icns --prune=true --out=release-builds"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^4.1.0",
    "electron-packager": "^13.1.1"
  },
  "dependencies": {
    "electron-window-state": "^5.0.3",
    "green-tunnel": "^1.0.2",
    "jquery": "^3.3.1"
  }
}
