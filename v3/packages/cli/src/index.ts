/**
 * Library surface of the `green-tunnel` package.
 *
 * v2 published the engine under this name, so `import { Proxy } from
 * 'green-tunnel'` keeps working — it is now a re-export of
 * `@green-tunnel/core`.
 */
export * from '@green-tunnel/core';
export { VERSION } from './version.js';
