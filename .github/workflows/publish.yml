---
name: Publish

# v2 used `on: create` with a `tags:` filter, which the create event does not
# support — it fired for every branch too. A tag push is the real trigger.
on:
  push:
    tags:
      - v*

jobs:
  verify:
    name: Verify tag matches package version
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@v4
      - id: version
        run: |
          TAG="${GITHUB_REF#refs/tags/v}"
          PKG=$(node -p "require('./packages/cli/package.json').version")
          CORE=$(node -p "require('./packages/core/package.json').version")
          echo "tag=$TAG cli=$PKG core=$CORE"
          if [ "$TAG" != "$PKG" ] || [ "$TAG" != "$CORE" ]; then
            echo "::error::Tag v$TAG does not match packages/cli ($PKG) / packages/core ($CORE)"
            exit 1
          fi
          echo "version=$TAG" >> $GITHUB_OUTPUT

  publish-npm:
    name: Publish to npm
    needs: [verify]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # npm provenance
    env:
      ELECTRON_SKIP_BINARY_DOWNLOAD: '1'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
          registry-url: https://registry.npmjs.org/

      - run: npm ci
      - run: npx tsc --build packages/cli

      # Order matters: `green-tunnel` depends on this exact version of the core.
      - name: Publish @green-tunnel/core
        run: npm publish --workspace packages/core --access public --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish green-tunnel
        run: npm publish --workspace packages/cli --access public --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  publish-docker-hub:
    name: Publish to Docker Hub
    needs: [verify]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # One multi-arch manifest instead of v2's separate `arm-*` tags built from
      # a sed-patched Dockerfile: `docker run sadeghhayeri/green-tunnel` now
      # picks the right architecture by itself. 32-bit ARM (Pi 2/3 on a 32-bit
      # OS) is gone — `node:24-alpine` has no armv7 variant to build on.
      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          file: Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          tags: |
            sadeghhayeri/green-tunnel:${{ needs.verify.outputs.version }}
            sadeghhayeri/green-tunnel:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-desktop:
    name: Build desktop (${{ matrix.os }})
    needs: [verify]
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            script: package:mac
            artifact: builds-mac
            glob: |
              apps/desktop/release/*.dmg
              apps/desktop/release/*.zip
          - os: windows-latest
            script: package:win
            artifact: builds-windows
            glob: apps/desktop/release/*.exe
          - os: ubuntu-latest
            script: package:linux
            artifact: builds-linux
            glob: |
              apps/desktop/release/*.AppImage
              apps/desktop/release/*.deb
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm

      - run: npm ci

      - name: Package
        run: npm run ${{ matrix.script }} --workspace apps/desktop -- --publish never
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # No Developer ID certificate in CI — without this, electron-builder
          # finds an unusable identity and fails instead of shipping unsigned.
          CSC_IDENTITY_AUTO_DISCOVERY: false

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.glob }}
          if-no-files-found: error

  new-release:
    name: Create GitHub release
    needs: [publish-npm, build-desktop]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: builds-*
          merge-multiple: true
          path: builds/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          draft: true
          generate_release_notes: true
          files: builds/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
